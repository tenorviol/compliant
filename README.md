A new Semantic Versioning library.

The goals are:
- Implement the SemVer spec as closely as possible
- Handle the precedence of pre-release builds accurately
- Handle comparisions that include build metadata correctly
